(function() {
    var groupOverviewService = function (localStorageService, calendarProvider) {
        var self = this;
    };

    angular.module('boligdrift').service('groupOverviewService', ['localStorageService', 'calendarProvider', groupOverviewService]);
})();